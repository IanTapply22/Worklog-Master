enum TaskType {
    setback = '[setback]',
    problem = '[problem]',
    realization = '[realization]',
    decision = '[decision]',
    plan = '[plan]',
    missed = '[missed]',
    direction = '[direction]',
    learning = '[learning]',
    conflict = '[conflict]',
    resolution = '[resolution]',
}